class Deal {
  String name, imageUrl;
  double prevPrice, newPrice, rate;
  int soldCount , discount;

  Deal(this.name, this.imageUrl, this.prevPrice, this.newPrice, this.rate,
      this.soldCount, this.discount);
}
